import { ChatGoogleGenerativeAI } from '@langchain/google-genai'; import { ChatPromptTemplate } from '@langchain/core/prompts'; import 'dotenv/config'; const llm = new ChatGoogleGenerativeAI({ model: 'gemini-2.5-flash-lite', apiKey: process.env.GEMINI_API_KEY }); const prompt = ChatPromptTemplate.fromMessages([['human', '{input}']]); prompt.formatMessages({ input: [{ type: 'text', text: 'what is this?' }] }).then(m => console.log(JSON.stringify(m, null, 2)));
